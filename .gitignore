# Build output
/build
/app/build
.gradle
.kotlin
local.properties

# Release signing material. The keystore and its passwords are machine-local:
# publishing them would let anyone ship an update that Android accepts as this
# app. `assembleRelease` degrades to an unsigned APK when they are absent.
keystore.properties
*.keystore
*.jks

# IDE
.idea
*.iml
.DS_Store

# Internal working notes and assets, not part of the published project.
/HANDOFF.md
/ads-handoff.md
/redesign.md
/ad/
/tutorial/
