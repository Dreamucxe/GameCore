# Build output
/build
/app/build
.gradle
.kotlin
local.properties

# Release signing material. The keystore and its passwords are machine-local:
# publishing them would let anyone ship an update that Android accepts as this
# app. `assembleRelease` degrades to an unsigned APK when they are absent.
keystore.properties
*.keystore
*.jks

# IDE
.idea
*.iml
.DS_Store

# Internal working notes and assets, not part of the published project.
/HANDOFF.md
/ads-handoff.md
/redesign.md
/ad/
/tutorial/

# local Gradle gate logs (never committed)
gate_*.log
*_build.log

# Removed ESP/figure-highlight package — deleted 2026-09-25 and must never be
# recreated or tracked again. Guardrail so the folder can never re-enter git.
**/core/vision/

# Internal working docs for the in-flight feature runs — never published.
/GraphicsTools_Progress.md
/GraphicsTools_Audit.md
/InstantReplay_Progress.md
/GC_ConfigEditor_Handover.md
